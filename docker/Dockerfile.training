FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN python -m pip install --no-cache-dir \
    --index-url https://pypi.org/simple \
    --trusted-host pypi.org \
    --trusted-host files.pythonhosted.org \
    -r requirements.txt

# Training reads Excel sources and serializes sklearn models through skops.
RUN python -m pip install --no-cache-dir "openpyxl>=3.1" skops

COPY src/ ./src/
COPY training/ ./training/
COPY configs/ ./configs/
COPY data/ ./data/

ENV PYTHONPATH="/app/src:/app"

# Default: train sklearn models
CMD ["python", "training/dhompo/train_sklearn.py"]
