# Optional dependencies for the Tier-A adaptive PyTorch backend.
#
# Install the base project dependencies first:
#   python -m pip install -e ".[dev]"
#
# This file intentionally contains only torch-specific dependencies. Core
# packages such as numpy, pandas, scikit-learn, FastAPI, and MLflow are declared
# in pyproject.toml.
#
# CPU-only PyTorch wheels. For GPU/CUDA deployment, replace this index URL with
# the CUDA wheel index that matches the target machine.
--index-url https://download.pytorch.org/whl/cpu

torch>=2.2,<3.0
